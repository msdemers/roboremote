# V2 Deployment (Fly.io)

This directory contains Fly.io deployment stubs for each service.
V1 targets local Docker Compose only. V2 will add:

- fly.toml per service
- GitHub Actions CI/CD pipeline
- Environment variable management via fly secrets

See .env.example for required environment variables.
